<url>
    <loc>{{ page.uri }}</loc>
    <lastmod>{{ page.updated }}</lastmod>
    <changefreq>{{ changefreq|default('monthly') }}</changefreq>
    <priority>{{ priority|default(0.7) }}</priority>
</url>